# راهنمای کامل پنل @vahidekhlasi (پنل رسمی)

این راهنما شما را قدم‌به‌قدم از نصب تا مدیریت روزانه می‌برد. در انتها یک بخش «سوالات متداول» از ربات پشتیبانی موجود است.

---

## پیش‌نیازها

- یک حساب vip در Cloudflare (اگر ندارید: https://dash.cloudflare.com/sign-up ، یک دقیقه).
- (اختیاری) یک دامنه شخصی برای پایداری و سرعت بیشتر، مخصوصاً برای gRPC/XHTTP و تماس‌ها.

---

## گام ۱: نصب پنل

سه راهنمای امن برای نصب در https://github.com/ekhlasi1/vahid/setup/ قرار دارد. سایت رسمی هیچ رمز Cloudflare یا API Token از شما دریافت نمی‌کند.

1. **نصب مستقیم Cloudflare (پیشنهادی):** دکمهٔ رسمی Deploy to Cloudflare منابع موردنیاز را داخل حساب خودتان می‌سازد. ورود و تأیید به صورت امن انجام می‌شود.
2. **ربات رسمی تلگرام:** ربات @vahidekhlasi_Bot مراحل نصب یا به‌روزرسانی را راهنمایی می‌کند. نام کاربری ربات را دقیقاً بررسی کنید.
3. **دستی با wrangler:** برای کسانی که می‌خواهند خودشان از مخزن https://github.com/ekhlasi1/vahid دیپلوی کنند.

نکته امنیتی: رمز حساب Cloudflare یا API Token را هرگز در سایت‌های عمومی وارد نکنید.

---

## گام ۲: اولین ورود و تعیین رمز ادمین

بعد از نصب، آدرس Worker خود را باز کنید؛ ویزارد نصب از شما می‌خواهد یک رمز ادمین بگذارید. این رمز برای ورود به پنل استفاده می‌شود. آن را امن نگه دارید.

---

## گام ۳: نگاهی به داشبورد

بالای داشبورد چهار کارت می‌بینید: وضعیت شبکه، حجم دیتابیس، مصرف Worker (تعداد Request امروز از سقف روزانه) و وضعیت کاربران.

**منظور از Request:** هر بار که یک کاربر یا برنامه درخواستی به Worker می‌فرستد، یک Request ثبت می‌شود.

---

(باقی راهنما با جایگزینی ارجاعات به ریسپانسی شما و تغییر کلمات "رایگان" به "vip" در متن فارسی انجام شده)

## سوالات متداول (FAQ)

_برگرفته از ربات پشتیبانی._

<details>
<summary><b>پنل چیه؟</b></summary>

این پنل (@vahidekhlasi) یک پنل گرافیکی vip و قدرتمند مبتنی بر Cloudflare Workers هست که به شما اجازه می‌ده پروکسی شخصی بسازید.

</details>

(بقیه جزئیات داخل GUIDE.md نیز لینک‌ها و عبارات به ریسپانسی جدید اشاره می‌کنند)
